name: Shell

on:
  push:
  pull_request:

jobs:
  syntax:
    name: POSIX shell syntax
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: sh -n
        run: |
          sh -n install.sh
          sh -n uninstall.sh
          sh -n bin/claude-horizon
          sh -n bin/claude-horizon-config
          sh -n bin/claude-horizon-models
          sh -n extras/https-time-sync

      - name: shellcheck
        run: |
          sudo apt-get update
          sudo apt-get install -y shellcheck
          shellcheck install.sh uninstall.sh bin/claude-horizon bin/claude-horizon-config bin/claude-horizon-models extras/https-time-sync

  powershell:
    name: PowerShell syntax
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Parse scripts
        shell: pwsh
        run: |
          $scripts = @(
            'install.ps1',
            'uninstall.ps1',
            'bin/windows/claude-horizon.ps1',
            'bin/windows/claude-horizon-config.ps1',
            'bin/windows/claude-horizon-models.ps1'
          )
          foreach ($script in $scripts) {
            [void][scriptblock]::Create((Get-Content $script -Raw))
          }
