# Optional one-shot runtime environment.
# Prefer `claude-deepseek-config` for normal local use.

DEEPSEEK_API_KEY=sk-your-deepseek-api-key

# Override the default model for a single environment if needed.
# ANTHROPIC_MODEL=deepseek-v4-pro[1m]
